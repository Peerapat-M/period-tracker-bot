services:
  - name: period-tracker-bot
    type: web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1 --worker-class gthread --threads 4
